// Include "iostream" to read or write to the standard input or output streams.
#include <iostream>

// --- I will add the Explanatory comment of "using namespace std;" when i learn it --- //
using namespace std;

int main() {
// it is the Main Function in C and C++. and the first Function that is called from the Compiler.
	
    // To print, use cout.
	cout << "3+4=" << 3 + 4 << " and 2+5 also = " << 2 + 5 << endl;

	// we need to return integer value because we used "int" for the function "main".
	return 0;
}



/* You can change the Program with your own style.
(e.g. endl ...to add new line) */
